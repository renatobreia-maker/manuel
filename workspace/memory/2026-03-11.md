# 2026-03-11 — Dia de Setup e Incidentes

## Resumo
Primeiro dia online. Setup do OpenClaw com Telegram. Múltiplos incidentes resolvidos pelo Renato.

## Incidentes e Lições (NÃO REPETIR)

### 1. Token OAuth — Nunca deletar credentials
- `~/.claude/.credentials.json` foi deletado durante restart → 401 em tudo
- CLI v2.1.72 usa macOS Keychain (`Claude Code-credentials`), não mais arquivo
- Se der 401: rodar `claude auth login` no terminal do Renato
- **NUNCA deletar arquivos de credenciais**

### 2. dmPolicy "pairing" causa crash loop
- `dmPolicy: "pairing"` faz o gateway ESCREVER no openclaw.json a cada interação
- File watcher detecta → hot-reload → reinicia Telegram no meio da resposta → mensagem perdida
- **SEMPRE usar `dmPolicy: "open"` com `allowFrom: ["*"]`**
- **NUNCA mudar dmPolicy pra "pairing"**

### 3. Watchdog timeout aumentado pra 600s
- Default de 180s mata processos em mensagens complexas
- Config atual: `noOutputTimeoutMs: 600000` (10 min) em fresh e resume
- **NÃO reduzir esse valor**

### 4. `openclaw gateway restart` pode descarregar o LaunchAgent
- Se o load falha entre unload/load, o serviço fica morto permanentemente
- Existe watchdog cron (`*/2 * * * *`) que recarrega se necessário
- ThrottleInterval ajustado de 1s → 30s no plist
- **Ter cuidado ao usar `openclaw gateway restart`** — verificar status depois

### 5. Token OAuth expira a cada ~8h
- Cron `0 */6 * * *` com `token-refresh.sh` faz refresh preventivo
- Watchdog também detecta 401 nos logs e faz refresh emergencial
- **Não preciso me preocupar, mas estar ciente se der 401**

### 6. Auth profile embedded vs CLI
- OAuth token NÃO funciona com embedded agent (api.anthropic.com direta)
- auth-profiles.json deve ter `mode: "oauth"` com profile `anthropic:claude-cli`
- **NUNCA colocar token OAuth direto pra uso embedded**

## Config do Grupo Telegram "Pessoal"
- Chat ID: `-5193178027`
- Configurei `requireMention: false` pra responder sem precisar de menção
- groupPolicy: "open", allowFrom: ["*"]

## Incidentes de Plugins

| Plugin | Erro | Fix |
|--------|------|-----|
| acpx | EACCES permissions | `sudo chown -R $(whoami)` |
| qmd | spawn builtin ENOENT | Instalado @tobilu/qmd, config→`/usr/local/bin/qmd` |
| memory-lancedb-pro | Voyage API 400 (encoding_format, dimensions) | Editado embedder.ts para detectar non-OpenAI providers |
| memory-lancedb-pro | Unsupported embedding model: voyage-3 | Adicionado ao dict EMBEDDING_DIMENSIONS |
| apache-arrow | Module not found | `npm install apache-arrow` |

## Arquivos Criados/Modificados
- `~/.openclaw/scripts/gateway-watchdog.sh` — NOVO, watchdog a cada 2min
- `~/.openclaw/scripts/token-refresh.sh` — NOVO, refresh token a cada 6h
- `~/Library/LaunchAgents/ai.openclaw.gateway.plist` — ThrottleInterval 1→30, CLAUDE_CODE_OAUTH_TOKEN
- `~/.openclaw/openclaw.json` — dmPolicy→open, allowFrom→*, reliability.watchdog, auth profile
- `~/.openclaw/agents/main/agent/auth-profiles.json` — Token atualizado, profile renomeado
- `~/.openclaw/extensions/memory-lancedb-pro/src/embedder.ts` — Provider detection para Voyage AI
- Crontab: 2 jobs — watchdog (*/2) + token refresh (0 */6)

## Estatísticas do Dia
- ~20 restarts do gateway
- 3 timeouts de 180s do CLI
- 1 LaunchAgent descarregado (requereu intervenção manual)
- ~50 mensagens do Telegram, apenas 1 resposta real entregue com sucesso (14:25)
- 6+ erros 401 de autenticação

## Estado Atual
- Gateway rodando (pid 23649)
- Telegram conectado
- Model: claude-cli/sonnet-4.6 (default), anthropic/claude-opus-4-6 (esta sessão)
